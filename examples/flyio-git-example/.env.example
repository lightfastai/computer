# Fly.io Configuration (Required)
FLY_API_TOKEN=your_fly_api_token_here
FLY_APP_NAME=lightfast-worker-instances

# Optional Configuration
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env.local
# 2. Get your Fly.io API token from: https://fly.io/user/personal_access_tokens
# 3. Replace 'your_fly_api_token_here' with your actual token
# 4. Update FLY_APP_NAME if you're using a different Fly.io app name

# Note: The provider pattern requires both FLY_API_TOKEN and FLY_APP_NAME
