# Development environment variables
# These are used during development but should be overridden in .env.local

# This is a placeholder - you need to set your actual token in .env.local
VERCEL_TOKEN=your_vercel_token_here

# Optional configurations (can be left empty for basic usage)
VERCEL_PROJECT_ID=
VERCEL_TEAM_ID=